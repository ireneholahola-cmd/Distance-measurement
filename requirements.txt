# Default install for local inference on CPU or machines without a dedicated NVIDIA setup.
# If you already installed a CUDA-enabled PyTorch manually, prefer `requirements_gpu.txt`.

-r requirements_common.txt

torch>=2.2.0,<3.0
torchvision>=0.17.0,<1.0

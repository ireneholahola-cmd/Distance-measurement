# 默认安装方式：用于 CPU 运行推理，或电脑没有 NVIDIA 独显的情况。
# 如果你已经手动装过 GPU 版 PyTorch，那就用 GPU 版的依赖文件 requirements_gpu.txt。

-r requirements_common.txt

torch>=2.2.0,<3.0
torchvision>=0.17.0,<1.0
opencv-python-headless>=4.8.0,<5.0
